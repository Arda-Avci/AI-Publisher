# Prod Plan 2 — Mixed Mode (Colab Docker + GCP Node.js)

## Amaç

Colab GPU + GCP CPU. Docker container'lar Colab'da, Node.js GCP'de. `WORKER_MODE=colab` → `gcp` ile full GCP'ye geçiş.

Detaylı plan: `.opencode\plans\prod_plan2_mixed.md`
